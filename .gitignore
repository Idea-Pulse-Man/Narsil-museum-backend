node_modules/
dist/
# Never commit env files or their backups (they hold real secrets).
# `.env.example` stays tracked — gitignore has no effect on already-tracked files.
.env
.env.*
*.bak
*.log
npm-debug.log*
.DS_Store

# Ingestion resume cursor (per-machine state)
.ingest-state.json

# Cached Met Open Access CSV (~318 MB, re-downloadable)
.met-cache/
